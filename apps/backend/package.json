{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "lint": "cargo clippy -- -D warnings",
    "check-types": "cargo check",
    "test": "./scripts/with-cleanup.sh cargo test",
    "test:coverage": "./scripts/with-cleanup.sh cargo llvm-cov --ignore-filename-regex 'src/main\\.rs' --fail-under-lines 90"
  }
}
