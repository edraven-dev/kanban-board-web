[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "=0.8.9"
tokio = { version = "=1.52.3", features = ["rt-multi-thread", "macros", "net"] }
tower-http = { version = "=0.7.0", features = ["cors"] }
